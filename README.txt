'npm install'
	-> pobranie bibliotek, utworzeni pliku package-lock.json

'npm start'
	-> uruchomienie aplikacji w trybie deweloperskim (http://localhost:3000)

'npm run build'
	-> build aplikacji, która jest później gotowa do zdeployowania
